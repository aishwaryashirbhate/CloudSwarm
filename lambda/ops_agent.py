def lambda_handler(event, context):
    """
    Ops Agent placeholder function.

    Args:
        event (dict): Event data.
        context (LambdaContext): Runtime info.

    Returns:
        dict: Response.
    """
    print("Ops Agent event:", event)
    return {"status": "ok"}
